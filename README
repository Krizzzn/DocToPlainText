[DocToPlainText]

a simple and not so fail proof c# console application, that is able to convert a winword document into a plain text file. 
It is meant to be used in script that parses a word document.

[[Dependencies]]
The solution depends on an installed word application (it has been tested with winword 2007).

[[Usage]]
foo.exe c:/input.doc c:/output.txt

